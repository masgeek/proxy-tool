handle_errors {
	@service_unavailable expression {http.error.status_code} in [502, 503, 504]
	header Retry-After "120"
	header X-Request-ID "{http.request.uuid}"
	root * /var/www/caddy-errors
	rewrite * /upstream-unavailable.html
	file_server
}
