# =============================================================================
# Caddy configuration for Beszel
# Copy the block into the host Caddyfile.
# =============================================================================

beszel.munywele.co.ke {
        tls dev@munywele.co.ke

        log {
                output file /var/log/caddy/beszel.log
                format console
                level info
        }

        reverse_proxy 127.0.0.1:8090
}
